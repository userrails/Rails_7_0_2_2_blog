<h1>Todos</h1>

<table border="1">
    <thead>
        <th>Title</th>
    </thead>
  <% @todos.each do |todo| %>
    <tbody>
        <td><%= todo.title %></td>
        <td><%= link_to "Show", todo_path(todo) %></td>
    </tbody>
  <% end %>
</table>

<%= link_to "New todo", new_todo_path %>